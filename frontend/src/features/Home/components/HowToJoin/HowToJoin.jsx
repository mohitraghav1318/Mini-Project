"use client";
import {useTranslations} from "next-intl";import {UserRoundPlus,Compass,Sprout} from "lucide-react";import styles from "./HowToJoin.module.scss";const icons=[UserRoundPlus,Compass,Sprout];export default function HowToJoin(){const t=useTranslations("home.how"),steps=t.raw("steps");return <section id="how-to-join" className={styles.section}><header><p>{t("eyebrow")}</p><h2>{t("title")}</h2><span>{t("description")}</span></header><div className={styles.steps}>{steps.map((step,index)=>{const Icon=icons[index];return <article key={step.number}><span className={styles.number}>{step.number}</span><span className={styles.icon}><Icon size={25}/></span><h3>{step.title}</h3><p>{step.description}</p></article>})}</div></section>}
